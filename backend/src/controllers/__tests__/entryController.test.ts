import {
  createEntry,
  getEntries,
  getEntry,
  updateEntry,
  deleteEntry,
} from "../entryController";
import { prismaMock } from "../../singleton";

describe("Entry Controller", () => {
  it("placeholder test", () => {
    expect(true).toBe(true);
  });
});
